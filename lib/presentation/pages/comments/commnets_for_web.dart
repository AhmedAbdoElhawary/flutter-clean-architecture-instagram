import 'package:flutter/material.dart';

class CommentsForWeb extends StatelessWidget {
  const CommentsForWeb({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
